/** Browser-only reconnect availability signals. */
export {browserReconnectSignals} from "@/reconnect/browser.js";
export type {
    RSocketBrowserReconnectSignals,
    RSocketBrowserWakeListener
} from "@/reconnect/browser.js";
